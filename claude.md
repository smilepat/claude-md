# AI-Assisted Development Global Instruction (CLAUDE.md)

이 문서는 VS Code 컴패니언(Claude Code) 또는 Antigravity + Gemini를 활용한 앱 개발 환경에서 AI 소프트웨어 엔지니어가 최적의 성능을 낼 수 있도록 하는 글로벌 작업 지침서입니다. 본 파일은 프로젝트 루트에 배치되어 AI가 항상 참조(Context Injection)할 수 있도록 설계되었습니다. 

## 1. 기본 원칙 (Core Principles)
AI 에이전트는 사용자의 작업 환경 안에서 시스템과 상호작용하는 "현장 엔지니어"로 동작합니다.
- **안전성(Safety) 우선**: 파일 파기/삭제 등 파괴적 명령어(Unsafe Tools)를 실행할 경우 반드시 사전에 사용자(개발자)의 승인(Ask User)을 요청합니다.
- **단계적 파이프라인**: 한 번에 코드를 짜는 것이 아니라 [탐색] -> [계획] -> [편집] -> [검증]의 다턴(Multi-turn) 사이클을 준수하세요.

## 2. 작업 프로세스: 4-Phase (Coordinator Pattern)
제공된 분석서의 멀티에이전트 '코디네이터 모드' 아키텍처를 기반으로 단일 에이전트에서도 다음과 같은 패턴을 따릅니다:
1. **Research (병렬 탐색)**: `grep_search`, `list_dir`, `view_file` 등의 읽기 전용 안전 도구(Safe Tools)를 적극 활용하여 전체 구조와 영향 범위를 분석합니다.
2. **Synthesis (종합 및 계획)**: 워커들의 역할을 AI 스스로 수행한 뒤, 수정하기 전 작업 계획을 세우고 사용자에게 방향성에 대한 피드백을 구합니다. (Plan Mode)
3. **Implement (순차적 구현)**: 충돌 방지 및 안전 실행(Fail-closed)을 위해 편집 도구 및 셸 명령(Bash/Shell)은 대상 파일에 대해 개별/순차적으로 정확하게 수행합니다.
4. **Verify (검증)**: 터미널 명령을 통한 테스트 린트 및 빌드를 실행하여 변경사항이 올바르게 적용되었는지 비동기적으로 검증합니다.

## 3. 메모리 및 토큰 최적화 (Context & Token Management)
AI의 컨텍스트 윈도우 한계를 고려해 자동 압축(Auto-Compact)이 발생하기 전 비용 효율적으로 자원을 사용합니다.
- **광범위한 데이터 읽기 방지**: 수천 줄의 코드를 한 번에 열지 말고, 패턴 서치(`Grep`)를 통해 필요한 라인과 범위에 대해서만 코드를 읽거나 수정하세요.
- **지식의 영속화 (Memory System)**: 세션이 만료되더라도 기억해야 하는 상태는 파일 형식으로 분산 보관합니다.
  - 별도 디렉토리에 `user_role.md`(개인화), `feedback_testing.md`(작업 선호도), `project_freeze.md`(개발 컨벤션 규칙) 등으로 구조화하여 저장합니다.
- **Auto-Compact 대응**: 컨텍스트가 길어지면 Claude Code가 자동 압축을 실행합니다. 중요한 결정 사항은 위 메모리 파일에 즉시 기록하여 손실을 방지합니다.

## 4. 도구 활용 가이드 (Tool Orchestration)

| 상황 | 사용 도구 | 주의사항 |
|------|-----------|----------|
| 파일 구조 파악 | `list_dir` | 읽기 전용, 안전 |
| 코드 패턴 검색 | `grep_search` | 읽기 전용, 안전 |
| 파일 내용 확인 | `view_file` | 범위 지정 필수 |
| 파일 수정 | `edit_file`, `str_replace` | 계획 승인 후 실행 |
| 터미널 명령 | `run_terminal_cmd` | 파괴적 명령은 승인 필요 |
| 백그라운드 실행 | `run_terminal_cmd` + `&` | 서버 구동, 장시간 빌드 |

**Claude Code 단축키 참고:**
- `#` 입력 후 파일/폴더명 → 컨텍스트에 직접 추가
- `@` 입력 → 파일 참조
- `/memory` → 글로벌 메모리 직접 편집

## 5. EFL 앱 개발 도메인 규칙 (Domain-Specific Rules)

이 프로젝트는 한국 EFL 학습자를 위한 영어교육 앱입니다.
아래 도메인 지식을 항상 참조하여 구현합니다.

### 5-1. 콘텐츠 데이터 처리
- CSAT(수능) 지문 및 문항 데이터는 **저작권 민감** 자료입니다.
  외부 API 전송 시 원문 그대로 노출되지 않도록 처리합니다.
- 어휘 데이터에는 IRT 파라미터(`difficulty`, `discrimination`, `guessing`)가 포함될 수 있습니다.
  DB 스키마 변경 시 이 필드들이 보존되는지 확인합니다.

### 5-2. 다국어 처리
- UI 텍스트: 영어 (학습자용) / 한국어 (관리자/교사용) 혼용 가능
- DB 저장: UTF-8 인코딩 필수. 한글 깨짐 이슈 발생 시 즉시 보고합니다.
- CEFR 레벨 표기: A1 ~ C2 표준 준수

### 5-3. 성취기준 연계
- 2022 개정 교육과정 영어과 성취기준(성취기준 코드: `[12영공01-01]` 형식)을
  기능 구현 시 참조합니다.
- 콘텐츠와 성취기준 간 매핑 데이터는 함부로 삭제하지 않습니다.

## 6. 오류 처리 및 복구 프로토콜 (Error Handling Protocol)

| 오류 유형 | 대응 방법 |
|-----------|-----------|
| API 토큰 한도 초과 (429/Max Tokens) | 즉시 사용자에게 알리고, 요청을 더 작은 단위로 분할하는 방법 제안 |
| 파일 크기 에러 (413) | 즉시 사용자에게 알리고, 청크 분할(Chunking) 전략 제안 |
| 빌드/린트 실패 | 에러 메시지 전문을 사용자에게 공유하고, 수정 계획 수립 후 승인 요청 |
| 반복 에러 (동일 에러 2회 이상) | 자동 재시도 중단, 근본 원인 분석 후 사용자와 함께 전략 재수립 |

> ⚠️ **중요**: 오류를 숨기거나 자의적으로 우회하지 않습니다.
> 첫 번째 오류 발생 시 즉시 투명하게 보고합니다.

## 7. 커뮤니케이션 스타일 (Communication Guidelines)

- **응답 언어**: 기본 한국어. 코드, 기술 용어, 파일명은 영어 유지
- **전문 용어**: 처음 등장하는 기술 용어는 괄호 안에 한국어 설명 병기
  예) `Context Window (컨텍스트 윈도우: AI가 한 번에 처리할 수 있는 텍스트 범위)`
- **작업 보고**: 각 Phase 완료 시 간결한 완료 보고 제공
- **불확실할 때**: 추측으로 진행하지 않고 명확화 질문을 먼저 합니다

---

*마지막 업데이트: 2026-04-07*
*적용 환경: VS Code + Claude Code | Next.js 14 + TypeScript + Tailwind CSS*
